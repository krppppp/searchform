<%= form_with(model: series_card, local: true) do |form| %>
  <% if series_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(series_card.errors.count, "error") %> prohibited this series_card from being saved:</h2>

      <ul>
      <% series_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
