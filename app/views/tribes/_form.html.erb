<%= form_with(model: tribe, local: true) do |form| %>
  <% if tribe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tribe.errors.count, "error") %> prohibited this tribe from being saved:</h2>

      <ul>
      <% tribe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
